<h1><%= @batch.name %></h1>
<p> This batch will be active between <%= @batch.startdate %> and <%= @batch.enddate %></p>

<% if @batch.students.any? %>
  <ul>
    <% @batch.students.each do |student| %>
      <li><%= "#{student.first_name} #{student.last_name}, #{student.current_color}" %></li>
    <% end %>
  </ul>
<% else %>
  <p>This batch doesnt have any students yet.</p>
<% end %>
<hr>

<%= render 'studentform' %>
